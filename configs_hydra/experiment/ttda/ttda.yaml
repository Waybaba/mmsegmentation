# @package _global_

ttda_hook: 
  type: TTDAHook
  turn_on_adapt: true # for test source-only
  adapt_img_vis_freq: ???
  use_pseudo_label: ???
  slide_adapt: ???
  high_conf_mask:
    turn_on: true
    top_p: 0.1 # only keep top_p pixels, others set to 255

optim_wrapper:
  paramwise_cfg:
    custom_keys:
      backbone:
        lr_mult: 0.0 # does not train backbone


custom_hooks:
  - ${ttda_hook} # ! change ttda_hook.xxx so that could be logged in wandb

default_hooks:
  visualization:
    draw: ??? # true
    interval: ??? # draw seg figure
  logger:
    interval: 10

train_cfg:
  val_interval: 100 # not used since this is TTDA

load_from: ??? # ! load from mmseg model

# model:
#   test_cfg:
#     mode: slide

train: false
test: true
tags: [ttda_debug]
task_name: MMSEG