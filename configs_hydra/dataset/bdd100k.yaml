defaults:
  ./default.yaml

test_dataloader:
  dataset:
    data_prefix:
      img_path: images/10k/val
      seg_map_path: labels/sem_seg/masks/val
    data_root: /data/BDD/
    type: BDD100KDataset
train_dataloader:
  dataset:
    data_prefix:
      img_path: images
      seg_map_path: labels
    data_root: /data/GTA5
    type: GTADataset
