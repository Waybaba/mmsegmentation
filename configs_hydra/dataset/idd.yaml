defaults:
  - default.yaml

test_scale_list_: [1024,512] # TODO need to verify TODO first several is [1920,1080] but we taske a smaller here for memory
test_dataloader:
  dataset:
    data_prefix:
      img_path: leftImg8bit/val
      seg_map_path: gtFine/val
    data_root: /data/IDD/
    type: IDDDataset
